import restrictNumeric from 'dummy/utils/restrict-numeric';
import { module, test } from 'qunit';

module('Unit | Utility | restrict numeric');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restrictNumeric({});
  assert.ok(result);
});
