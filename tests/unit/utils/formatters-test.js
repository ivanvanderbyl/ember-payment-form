import formatters from 'dummy/utils/formatters';
import { module, test } from 'qunit';

module('Unit | Utility | formatters');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatters();
  assert.ok(result);
});
